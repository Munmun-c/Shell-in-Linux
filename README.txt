execute all the commands (ls, clear, vi etc) 
Shell built­ins (cd, pwd, export) 
Support for Pipes “|” (multiple) 
Support for I/O redirection (<, >) 
Print environment variables and text using echo 
On   pressing   "Ctrl+C",   the   command   that   is   running   currently  running should be terminated,Myprogram.c wil not terminate.
Support for history operator .
Background and foreground functionality : &, fg 
